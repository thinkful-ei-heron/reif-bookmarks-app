/* eslint-disable no-console */
export const Bookmarks = [];

export const addBookmark = (bookmark) => {
};

export const editBookmark = (id, newData) => {
};

export const deleteBookmark = (id) => {
};